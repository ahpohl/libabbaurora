branches:
  only:
    - master

addons:
  apt:
    packages:
      - doxygen

script:
  - make docs

deploy:
  provider: pages:git
  cleanup: true
  strategy: git
  local_dir: docs/html
  token: $GH_REPO_TOKEN
  edge: true
  on:
    branch: master
